---
// Layout principal de la aplicación
interface Props {
  title: string;
  description?: string;
}

const { title, description = "Registro de eventos juveniles" } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} - Paroikiapp</title>
    <style is:global>
      :root {
        --color-primary: #3b82f6;
        --color-secondary: #10b981;
        --color-danger: #ef4444;
        --color-gray-light: #f3f4f6;
        --color-gray-dark: #1f2937;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        color: var(--color-gray-dark);
        background-color: #f9fafb;
        line-height: 1.6;
      }

      a {
        color: var(--color-primary);
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      button {
        background-color: var(--color-primary);
        color: white;
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        cursor: pointer;
        font-size: 1rem;
      }

      button:hover {
        background-color: #2b5be8;
      }

      input, textarea, select {
        padding: 0.5rem;
        border: 1px solid #d1d5db;
        border-radius: 0.375rem;
        font-size: 1rem;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 1rem;
      }

      .error {
        color: var(--color-danger);
        background-color: #fee2e2;
        padding: 0.75rem;
        border-radius: 0.375rem;
        margin-bottom: 1rem;
      }

      .success {
        color: var(--color-secondary);
        background-color: #dcfce7;
        padding: 0.75rem;
        border-radius: 0.375rem;
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <nav style="background-color: var(--color-primary); color: white; padding: 1rem;">
      <div class="container">
        <a href="/" style="color: white; font-weight: bold; font-size: 1.5rem;">Paroikiapp</a>
      </div>
    </nav>

    <div class="container">
      <slot />
    </div>
  </body>
</html>
